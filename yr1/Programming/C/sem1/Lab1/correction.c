#include <stdio.h>

main(){
    //Program to illustrate errors in a C program
    int i=5,j;
    float k;
    
    j=40000;
    
    printf("The value of i is %d\n", i);
    printf("Size of an integer is %d\n", sizeof(int));
    
    return 0;
}
